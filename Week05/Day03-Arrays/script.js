// const favouriteFood = ["Shepards pie", "Toad in the hole", "Sunday roast"];

// console.log(favouriteFood);

// favouriteFood[2] = "Gyros";

// console.log(favouriteFood[favouriteFood.length -1]);

const number = [1, 2, 3, 4, 5];

let average = (number[0] + number[1] + number[2] + number[3] + number[4]) / number.length;

console.log(average);

const favouriteRecipe = {
    title: "Toad in the hole",
    servings: 6,
    ingredients: ["Mince" , "Onion", "Potatoes", "Cheese", "Gravy"]
};

console.log(favouriteRecipe);

favouriteRecipe.ingredients.push("Peas");

console.log(favouriteRecipe);
